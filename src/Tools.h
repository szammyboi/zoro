#include <yaml-cpp/yaml.h>
#include <string>
#include <iostream> 
#include <vector>

// better wildcard system; double wildcards whatever;
std::vector<std::string> ParseList(const std::vector<std::string>& list);
